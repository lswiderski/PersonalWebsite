@using PersonalWebsite.Services.Models
@model List<TagViewModel>
@{
}
<table>
    <thead>
        <tr>
            <th>
                Tag
            </th>
            <th>
                Uses
            </th>
            <th>
            </th>
        </tr>
    </thead>
    <tbody>
        @foreach (var tag in Model)
        {
            <tr>
                <td><a href="@tag.TagId"> @tag.Name</a> fill form with data to edit</td>
                <td><a href="@tag.TagId"> @tag.Uses</a> redirect to page with all post uses this tag</td>
                <td><a href="@tag.TagId">X</a> delete</td>

            </tr>
        }
    </tbody>
</table>